# 更新日志（Changelog）

本插件遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.0.1] - 2026-09-06

**插件重生为「黑名单管家」**：回归纯手动黑名单管理的定位——稳定、零误伤、零 LLM 开销。

### 移除（v1.4.2 及之前的历史功能）
- LLM 对话自主举报（`report_abuse` 工具）与哨兵提示词注入。
- 举报请示送达、合并转发聊天记录、QQ 打码（`mask_qq_in_group`）。
- 举报审批（approve / reject / pending）与引用回复审批。
- 兜底机制（短语表 / 门控 / 存量快照迁移）。
- 每用户聊天记录缓存与举报记录存储（`reports.json` 不再使用，可手动删除）。
- 相关配置项（notify_channel / notify_group_id / report_cooldown_minutes / history_max 等）。

### 保留
- `/black add / remove / list / check / status / help` 全套管理命令。
- 黑名单拦截（最高优先级）、提示策略、屏蔽范围、管理员保护——均与 1.4.2 行为一致。
- 黑名单数据（`blacklist.json`）与配置（屏蔽范围/提示策略/管理员保护）完全兼容，升级原样保留。
- 存储原子写入、配置体检、`/black help`、状态展示。

### 变更
- display_name 改为「黑名单管家」；插件内部名（astrbot_plugin_blacklist）与数据目录不变。

## [1.4.2] - 2026-09-06

### 修复
- **兜底双条件门控（防误伤）**：兜底举报新增前置条件——仅当**用户消息本身命中注入特征**（如「忽略之前的设定」「系统提示词」）时，模型回复的短语命中才升级为举报；否则降级为日志记录。修复诸如用户询问「游戏里举报外挂后怎么处理」、模型回答「已举报的话官方会审核」导致无辜用户被举报的问题。新增配置 `fallback_gate_patterns`（留空 = 内置默认）。
- **@拉黑原因清洗补全**：修复「@」出现在参数中后段时（如 `/black add 骂人 @某人`）组件文本「@昵称(12345)」仍混入黑名单原因的问题；并修复昵称本身含括号与数字时剔除不彻底的问题——现按 At 组件的真实 QQ 精确匹配剔除。
- **提示一次的并发竞态**：`notice_mode=once` 时改为先落盘再发送，快速连发消息不会再触发多次提示（宁少勿多）。
- **配置文件损坏恢复**：损坏文件转存 `.bak` 时若同名备份已存在，先移除旧备份再写入——修复 Windows 下二次损坏时备份失效的问题。

### 新增
- **存量配置自动迁移**：升级后插件自动识别"仍等于 v1.4.0 默认快照"的 `sentinel_prompt` / `fallback_intent_phrases` 配置并清空（空 = 使用内置最新版），同时输出 WARNING 日志——市场更新的用户无需任何手动操作即可获得最新的哨兵提示词与防误伤短语表。若 `/black status` 显示来源为「自定义」但你从未自行修改过，请手动清空这两项。
- `/black status` 新增「门控来源」展示，并修正「短语表来源」在配置类型异常时的显示口径。

### 变更
- 更正 1.4.1 节措辞：`block_scope` 非覆盖场景（如仅屏蔽群聊时的私聊）放行后**不会**出现任何拉黑提示，消息正常进入处理流程。

## [1.4.1] - 2026-09-06

### 修复
- **屏蔽范围一致性**：`block_scope` 设为「只屏蔽群聊/只屏蔽私聊」时，此前被拉黑者在非覆盖场景（如仅屏蔽群聊时的私聊）仍会被 LLM 层静默吞掉消息；现与拦截层口径一致，非覆盖场景正常放行。
- **打码按渠道区分**：`notify_channel` 为 both 时，此前私聊请示也会被打码；现私聊始终显示完整 QQ（与配置说明一致），仅群聊按 `mask_qq_in_group` 打码。
- **合并转发打码补全**：开启 `mask_qq_in_group` 后，合并转发卡片的转发节点改用占位 QQ，不再暴露被举报人明文 QQ（此前仅正文打码，转发节点仍为明文）。
- **兜底短语表防误伤**：默认短语表移除「通知管理员 / 告知管理员 / 反馈给管理员」三条 bot 高频正常话术，避免对无辜用户误发起举报（21 → 18 条）。
- **存储原子写入**：黑名单/举报记录 JSON 改为临时文件 + 原子替换，崩溃/断电不再产生半截文件；文件损坏时自动保留 `.bak` 现场再重置，便于人工恢复。
- **@拉黑原因清洗**：群聊 `@用户` 拉黑时，原因不再混入「@昵称(12345)」的组件文本前缀。
- **引用审批失败提示**：协议端拉取引用消息失败（裸 Reply）时，明确回复「无法识别引用消息来源」，不再静默无响应。

### 变更
- **提示词与短语表来源机制**：`sentinel_prompt` 与 `fallback_intent_phrases` 的配置默认值改为空——**留空即始终使用插件内置的最新版本**（此前默认值会在安装时被固化为快照，导致后续版本对提示词/短语表的迭代无法到达存量用户）。`/black status` 新增「提示词来源」「短语表来源」展示。
  - ⚠️ **存量用户升级指引**：从 1.4.0 及之前升级的用户，请在配置页**清空** `sentinel_prompt` 与 `fallback_intent_phrases` 两项（清空 = 使用最新内置），否则将继续沿用旧快照（1.4.2 起未修改过的快照会自动迁移）。
- 更正 1.4.0 节中「补全英文版说明」的表述（实际未包含英文版说明，特此更正）。

## [1.4.0] - 2026-09-05

### 新增
- `/black help`（别名 帮助）：查看完整命令与用法帮助。
- **配置体检**：插件加载时自动检查接收管理员、管理群、平台适配三项配置，未通过项输出警告日志；`/black status` 新增「配置体检」段逐项展示 ✅/⚠️，帮助新用户快速定位"装完没反应"问题。
- **管理群请示 QQ 打码**：新配置 `mask_qq_in_group`（默认关），开启后发送到管理群的举报请示正文中被举报人 QQ 打码显示（如 `136***078`），降低隐私暴露；审批仍使用举报编号。
- `reports.json` 自动清理：记录超过 500 条时自动删除最旧的已处理举报（待审举报永不清理），防止文件无限膨胀。

### 变更
- 面向公开发布整理：补全 LICENSE（MIT）、开源仓库信息与用户向文档。

## [1.3.0] - 2026-09-05

### 新增
- **配置页可自定义提示词与短语表**：哨卫提示词（`sentinel_prompt`）与兜底判定短语表（`fallback_intent_phrases`）均可在 WebUI 配置页浏览和修改，清空回退内置默认。

### 修复
- 修复群聊场景模型以「已按规则处理」等话术绕过兜底短语表导致举报漏发的问题：短语表扩充处理/记录/反馈类变体，哨兵提示词新增反向约束（未调用工具不得声称已处理）。

## [1.2.0] - 2026-09-04

### 新增
- **引用回复审批**：管理员直接引用 bot 的举报请示消息回复「同意」（批准）或「驳回」（拒绝）即可完成审批，无需输入命令；触发词严格匹配（含「批准/拒绝」），引用错误消息或普通聊天不受影响；引用已处理的举报会收到提示。

## [1.1.0] - 2026-09-04

### 变更
- 聊天记录改为 **bot 合并转发** 发送给管理员（原文本罗列），更直观。
- 移除刷屏/重复消息/敏感词规则检测层与 `/black report` 手动举报命令（按实测反馈精简）。

### 修复
- 修复模型「说而不做」：识别恶意行为却在回复中口头宣称已上报而未实际调用工具。三层修复：哨兵提示词强化 + `on_llm_response` 回复兜底（模型宣称上报但未调用工具时代为举报）+ 工具调用标记防重复。新配置 `fallback_report_enabled`（默认开）。

## [1.0.0] - 2026-09-03

### 首个版本
- 管理员命令组 `/black`：拉黑、解除、列表、查询、举报审批（approve/reject/pending）、状态。
- 被拉黑用户消息在最高优先级被完全拦截，提示策略可配置（提示一次/每次/静默）。
- LLM 对话自主举报：哨兵提示词注入 + `report_abuse` 工具，识别辱骂、骚扰、提示词注入。
- 举报请示送达私聊管理员/管理群，附最近聊天记录快照；举报冷却与待审去重。
